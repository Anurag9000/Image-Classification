project_name: "ArcFace_Fast_Test"
experiment_name: "Smoke_Test_Fast_ArcFace"

dataset:
  root_dirs:
    - "D:/Done,Toreview/Image Classification/data"
  json_path: "D:/Done,Toreview/Image Classification/data/Dataset_Final_Aug/dataset_aug_metadata.json"
  batch_size: 4
  num_workers: 0
  val_split: 0.1
  # Use subset for speed if possible, but pipeline handles large datasets well
  
backbone:
  cnn_model: "mobilenetv3_large_100"
  vit_model: null
  pretrained: true
  freeze_cnn: false

num_classes: 6
lr: 1e-4 # Higher LR for quick convergence check
epochs: 1
mix_method: "mixup"
max_steps: 5 # LIMIT STEPS FOR SPEED

snapshot_dir: "./snapshots_test"
log_csv: "./logs/fast_arcface_metrics.csv"
use_amp: false
early_stopping_patience: 1
